
class Math {

  def add( a:Int, b:Int ): Int = {

    var sum:Int = 0
    sum = a + b
    return sum
  }

  def subtract(a:Int, b:Int): Int = {
    var sub:Int = 0
    sub = a - b
    return sub
  }

  def multiply(a:Int, b:Int): Int = {
    var mult:Int = 0
    mult = a * b
    return mult
  }
}
